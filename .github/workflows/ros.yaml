name: ROS

on:
  push:
    branches:
      - main

jobs:
  build-and-push-image:
    name: Build and push Docker image to Github container registry
    uses: san-soucie/sgdrfros/.github/workflows/docker.yml@main
    permissions:
      contents: read
      packages: write
  test:
    name: Test
    runs-on: ubuntu-latest
    container: ghcr.io/san-soucie/sgdrfros:main
    steps:
      - uses: actions/checkout@v2
      - name: Setup ROS tooling
        uses: ros-tooling/setup-ros@v0.6
        with:
          required-ros-distributions: iron
      - name: Run ROS ci
        uses: ros-tooling/action-ros-ci@v0.3
        with:
          target-ros2-distro: iron

  lint:
    name: Lint (ament_${{ matrix.linter }})
    runs-on: ubuntu-latest
    needs: build-and-push-image
    container: ghcr.io/san-soucie/sgdrfros:main
    strategy:
      fail-fast: false
      matrix:
        linter: [cppcheck, cpplint, uncrustify, lint_cmake, xmllint, flake8]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run linter
        uses: ./.github/actions/lint/
        env:
          LINTER: ${{ matrix.linter }}
  lint_pep257:
    name: Lint (ament_pep257)
    runs-on: ubuntu-latest
    needs: build-and-push-image
    container: ghcr.io/san-soucie/sgdrfros:main
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run linter
        run: |
          ament_pep257 --convention numpy sgdrf--add-ignore D100 D101 D102 D103 D104 CNL100 D213 D413 D419
